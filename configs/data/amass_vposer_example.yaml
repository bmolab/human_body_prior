# Example config for preparing AMASS data for VPoser training.
#
# Run from the repository root:
#   python amass_data_prep.py --config configs/data/amass_vposer_example.yaml


# Note: Replace the dataset names below with the AMASS subsets you have downloaded.

amass_dir: AMASS
out_dir: AMASS/DataSet/vposer_amass_example
log_name: prepare.log

splits:
  vald:
    - HumanEva
    - MPI_HDM05
    - SFU
    - MPI_mosh
  test:
    - Transitions_mocap
    - SSM_synced
  train:
    - CMU
    - MPI_Limits
    - TotalCapture
    - Eyes_Japan_Dataset
    - KIT
    - BML
    - EKUT
    - TCD_handMocap
    - ACCAD
